{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
{{- $lang := $.Language.Lang }}
{{- $langPrefix := printf "/%s/" $lang }}
{{- range site.AllPages }}
  {{- /* Hugo 0.162+ site.Pages scope is limited per language, so use AllPages + URL prefix filter */}}
  {{- /* Default language (en) uses no prefix; non-default languages use /{lang}/ prefix */}}
  {{- $include := false }}
  {{- if eq $lang "en" }}
    {{- $include = not (findRE `^/[a-z]{2}/` .RelPermalink) }}
  {{- else }}
    {{- $include = hasPrefix .RelPermalink $langPrefix }}
  {{- end }}
  {{- if $include }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}{{ if .IsTranslated }}
    {{- range .Translations }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.LanguageCode }}" href="{{ .Permalink }}" />{{- end }}
    <xhtml:link rel="alternate" hreflang="{{ .Language.LanguageCode }}" href="{{ .Permalink }}" />{{ end }}
  </url>
  {{- end }}
{{- end }}
</urlset>
